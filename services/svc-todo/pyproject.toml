[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "svc-todo"
version = "0.1.0"
description = "Amplifier todo tool service — AI-managed todo list for self-accountability"
requires-python = ">=3.11"
dependencies = [
    "amplifier-service-sdk",
]

[tool.uv.sources]
amplifier-service-sdk = { path = "../../amplifier-service-sdk" }

[tool.hatch.build.targets.wheel]
packages = ["src/svc_todo"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["src"]

[tool.pyright]
pythonVersion = "3.11"
extraPaths = ["src"]
venvPath = "."
venv = ".venv"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "httpx>=0.27",
]
